<mah:MetroWindow x:Class="SmartFactory.Presentation.Views.Shell.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Height="900" Width="1600"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource PrimaryBackgroundBrush}">

    <mah:MetroWindow.LeftWindowCommands>
        <mah:WindowCommands>
            <TextBlock Text="SmartFactory" FontWeight="Bold" FontSize="16"
                       VerticalAlignment="Center" Margin="8,0"/>
        </mah:WindowCommands>
    </mah:MetroWindow.LeftWindowCommands>

    <mah:MetroWindow.RightWindowCommands>
        <mah:WindowCommands>
            <!-- Factory Selector -->
            <ComboBox ItemsSource="{Binding Factories}"
                      SelectedItem="{Binding SelectedFactory}"
                      DisplayMemberPath="Name"
                      Width="200"
                      Margin="0,0,16,0"/>

            <!-- Alarm Indicator -->
            <Button Command="{Binding NavigateToAlarmsCommand}"
                    ToolTip="View Alarms"
                    Margin="0,0,8,0">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Bell" Width="16" Height="16"/>
                    <TextBlock Text="{Binding ActiveAlarmCount}"
                               Margin="4,0,0,0"
                               FontWeight="Bold"
                               Visibility="{Binding ActiveAlarmCount, Converter={StaticResource IntToVisibilityConverter}}"/>
                </StackPanel>
            </Button>

            <!-- Clock -->
            <TextBlock Text="{Binding CurrentTime, StringFormat='{}{0:HH:mm:ss}'}"
                       VerticalAlignment="Center"
                       FontFamily="Consolas"
                       Margin="8,0"/>
        </mah:WindowCommands>
    </mah:MetroWindow.RightWindowCommands>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Navigation Sidebar -->
        <Border Grid.Column="0"
                Background="{StaticResource SecondaryBackgroundBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,1,0">
            <ListBox ItemsSource="{Binding NavigationItems}"
                     SelectedItem="{Binding SelectedNavigationItem}"
                     Background="Transparent"
                     BorderThickness="0"
                     Padding="0,8">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem" BasedOn="{StaticResource NavigationItemStyle}"/>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="{Binding IconName}"
                                                        Width="20" Height="20"
                                                        VerticalAlignment="Center"
                                                        Foreground="{StaticResource SecondaryTextBrush}"/>
                            <TextBlock Text="{Binding Title}"
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center"
                                       FontSize="14"
                                       Foreground="{StaticResource PrimaryTextBrush}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Main Content Area -->
        <Border Grid.Column="1"
                Background="{StaticResource TertiaryBackgroundBrush}">
            <ContentControl Content="{Binding CurrentView}"/>
        </Border>
    </Grid>
</mah:MetroWindow>
